import React from "react";

export default function TaskComponent() {
    const [tasks, setTasks] = useState(null);

    return (
        <View>
            <Text>Task</Text>
        </View>
    );
};